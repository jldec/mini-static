{
	"name": "mini-static",
	"private": "true",
	"version": "1.0.0",
	"scripts": {
		"build": "mkdir -p dist && cp index.html dist",
		"deploy": "wrangler pages deploy dist"
	},
	"devDependencies": {
		"wrangler": "^4.76.0"
	}
}